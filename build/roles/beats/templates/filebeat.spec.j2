Name        : filebeat
Version     : {{ beats_version }}
Release     : 1
Group       : default
License     : ASL 2.0
Vendor      : Elastic
URL         : https://github.com/elastic/beats/filebeat
Summary     : Filebeat sends log files to Logstash or directly to Elasticsearch

%description
Filebeat sends log files to Logstash or directly to Elasticsearch

%post
#!/bin/bash
systemctl daemon-reload 2> /dev/null || true

%files
{% for file in beat_files['filebeat'].files %}
{{ file }}
{% endfor %}

