Name        : metricbeat
Version     : {{ beats_version }}
Release     : 1
Group       : default
License     : ASL 2.0
Vendor      : Elastic
URL         : https://github.com/elastic/beats/metricbeat
Summary     : Metricbeat sends metrics to Elasticsearch

%description
Metricbeat sends metrics to Elasticsearch

%post
#!/bin/bash
systemctl daemon-reload 2> /dev/null || true

%files
{% for file in beat_files['metricbeat'].files %}
{{ file }}
{% endfor %}

