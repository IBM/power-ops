# {{ ansible_managed }}
client:
  hosts:
    - {{ ansible_host }}
  port: "{{ elasticsearch_http_port }}"
  timeout: 30

logging:
  loglevel: INFO
  logfile: /var/log/curator.log
  logformat: default
